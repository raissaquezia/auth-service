# user-service
The User service manages user accounts and authentication for Flex Market.  It handles registration, login, user roles (customer, admin, seller), and JWT token generation for authentication.
